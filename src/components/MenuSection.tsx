import React from 'react';\nimport { Card } from '@/components/ui/card';\nimport { Button } from '@/components/ui/button';\nexport type Drink = { name: string; description: string; price: string; };\n\nconst DEFAULT_DRINKS: Drink[] = [\n  { name: 'Espresso', description: 'Ristretto shot, bold & intense', price: '$2.95' },\n  { name: 'Latte', description: 'Creamy milk, smooth', price: '$3.75' },\n  { name: 'Cappuccino', description: 'Espresso with frothy crown', price: '$3.50' },\n  { name: 'Flat White', description: 'Velvety microfoam', price: '$3.75' },\n];\n\ninterface MenuSectionProps { items?: Drink[]; }\n\nconst MenuSection: React.FC<MenuSectionProps> = ({ items }) => {\n  const drinks = items ?? DEFAULT_DRINKS;\n  return (\n    <section className=\"py-12\">\n      <div className=\"container mx-auto px-4\">\n        <h2 className=\"text-2xl font-semibold mb-6\">Popular Drinks</h2>\n        <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6\">\n          {drinks.map((d) => (\n            <Card key={d.name} className=\"p-4 flex flex-col\">\n              <div className=\"h-32 rounded-md bg-gradient-to-br from-yellow-100 to-yellow-300 flex items-center justify-center mb-2\">\n                <span className=\"text-3xl\">☕</span>\n              </div>\n              <h3 className=\"font-semibold mb-1\">{d.name}</h3>\n              <p className=\"text-sm text-muted-foreground mb-2\">{d.description}</p>\n              <div className=\"mt-auto flex items-center justify-between pt-2\">\n                <span className=\"font-bold\">{d.price}</span>\n                <Button size=\"sm\" variant=\"ghost\">Add</Button>\n              </div>\n            </Card>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n};\n\nexport default MenuSection;\nexport type { Drink };